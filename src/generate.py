"""Generation stage: render a decision into a markdown draft."""


def generate(decision: dict) -> str:
    """Render a reconciliation decision as a markdown report.

    Will use a Jinja2 template to turn the decision into a human-readable
    draft written to the output/ directory.

    Not implemented yet.
    """
    raise NotImplementedError
